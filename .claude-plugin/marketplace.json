{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "p3nj-market",
  "owner": {
    "name": "Benjamin Wang"
  },
  "plugins": [
    {
      "name": "datadog-log-analyst",
      "source": "./plugins/datadog-log-analysis",
      "description": "Analyse Datadog logs for Prismatic integrations with streaming accumulator for million-log datasets. Includes skills for fetching, analysing (SAP, AMT, Maximo, generic), and reporting to chat, Slack, Notion, or DOCX. Requires the Datadog Connector extension for MCP access."
    },
    {
      "name": "mcp-connector-builder",
      "source": "./plugins/mcp-connector-builder",
      "description": "Skill for building and packaging custom MCP servers as Claude Desktop Extension bundles (.mcpb). Guides you through scaffolding, manifest.json spec 0.3, mcpb CLI, and team distribution."
    },
    {
      "name": "http-toolkit",
      "source": "./plugins/http-toolkit",
      "description": "Generic AI-driven HTTP toolkit. Two strictly separated skills — http-call (one request in, one response out, all params per-call, supports JSON/XML bodies and bearer/basic/api_key/OAuth2 auth) and api-test (orchestrator that chains http-call steps, captures values across calls, and asserts results). Zero npm deps, auto-loads .env from CLAUDE_PROJECT_DIR or CWD."
    }
  ]
}
